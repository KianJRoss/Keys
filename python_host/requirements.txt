# Python dependencies for Keychron V1 Raw HID host software

# HID communication library
hidapi>=0.14.0

# Windows audio control (Core Audio API)
pycaw>=20220416

# Windows API access (window management, media keys)
pywin32>=305

# COM types (required by pycaw)
comtypes>=1.2.0

# System tray icon
pystray>=0.19.0
Pillow>=10.0.0

# Process and system utilities (for context detection)
psutil>=5.9.0

# Modern Qt-based overlay UI
PyQt6>=6.6.0
